### Install Package NPM
```
npm install
```


### Run the CLI tool to scan your template files for classes and build your CSS
```
npx tailwindcss -i ./src/styles.css -o ./dist/css/final.css --watch
```